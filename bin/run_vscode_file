# replace first instance
# ${parameter/pattern/string}

# replace all
# ${parameter//pattern/string}

main() {
  local input="$1"
  local find="\\"
  local replace="\/"
  local new_input=$(echo $input | sed 's#\\#\/#g')
  run_file "$new_input" ${@:2}
}

main "$@"
