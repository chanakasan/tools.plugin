#!/bin/env bash

main() {
  local trash_dir=$HOME/trash
  local a1="$1"
  local outName=$(basename $a1)

  if [ -z "$a1" ]; then
	  mux trash
  fi

  mkdir -p $trash_dir
  mv $a1 $trash_dir/$outName--`num`
}

main "$@"
